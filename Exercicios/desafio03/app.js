function Verificar(){
    numero = parseInt(prompt("Digite um numero inteiro qualquer? "))
    sucessor = numero + 1
    antecessor = numero - 1
    alert(`Antes do ${numero}, temos o número ${antecessor}\nDepois do ${numero}, temos o número ${sucessor}`)
}